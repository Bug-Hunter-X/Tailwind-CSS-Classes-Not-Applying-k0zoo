```javascript
// Correctly applying Tailwind CSS classes.
// Ensure Tailwind directives are correctly configured in your config file (tailwind.config.js) and that the correct import paths are used.
// Verify no conflicting CSS rules override Tailwind styles. 
<div class="bg-red-500 p-4">
  <p>This text should now be red and have padding.</p>
</div>
```